const LS_CURRENCY = 'globalOptions.currency ';
const LS_LANGUAGE = 'globalOptions.language ';
const LS_PERIODS_AS_DATES = 'globalOptions.periodsAsDates ';

export default Object.freeze({
  LS_CURRENCY,
  LS_LANGUAGE,
  LS_PERIODS_AS_DATES,
});
